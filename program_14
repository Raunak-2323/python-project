#Write a program that reads multiple lines of input from the user until they enter an empty line, then prints all the lines.

lines = []
while True:
    line = input("Enter a line of text (press Enter for a new line, or just Enter to finish): ")
    if line == "":
        break
    lines.append(line)

print("Lines you entered:")
for line in lines:
    print(line)